# continue and break don't work inside a forEach - use return instead.
# one object is not equal to another object in javascript.
# use str.toLowerCase() and str.toUpperCase() to change capitalization of a str.